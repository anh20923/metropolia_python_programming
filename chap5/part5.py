"""
| Mode   | Ý nghĩa                                                                                        |
| ------ | ---------------------------------------------------------------------------------------------- |
| `"r"`  | Read (đọc). Nếu file không tồn tại → báo lỗi                                                   |
| `"w"`  | Write (ghi). Nếu file không tồn tại → tạo mới. Nếu tồn tại → xóa toàn bộ nội dung cũ rồi ghi   |
| `"a"`  | Append (ghi thêm). Nếu file không tồn tại → tạo mới. Nếu tồn tại → thêm nội dung vào cuối file |
| `"rb"` | Đọc dữ liệu nhị phân (binary)                                                                  |
| `"wb"` | Ghi dữ liệu nhị phân (binary)                                                                  |

"""